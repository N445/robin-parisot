{% set page = page is defined?page:'nc' %}

<nav class="navbar navbar-expand-lg navbar-light bg-light shadow mb-5 sticky-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('HOMEPAGE') }}">Robin PARISOT</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link{{ page == 'skills'?' active':'' }}" aria-current="page" href="{{ path('SKILLS') }}">
                        Compétences
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{{ page == 'news'?' active':'' }}" aria-current="page" href="{{ path('NEWS') }}">
                        News
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{{ page == ''?' active':'' }}" aria-current="page" href="#">
                        Réalisation
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{{ page == 'contact'?' active':'' }}" aria-current="page" href="{{ path('CONTACT') }}">
                        Contact
                    </a>
                </li>
            </ul>
            <form class="d-flex">
                <div class="input-group">
                    <input class="form-control" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </form>
        </div>
    </div>
</nav>